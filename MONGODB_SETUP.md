# MongoDB Atlas Setup для Wave Chat

## 🗄️ Зачем нужна MongoDB?

Render.com **удаляет все файлы при каждом деплое**. Для постоянного хранения:
- ✅ История сообщений (24 часа)
- ✅ Зарегистрированные пользователи
- ✅ Список банов
- ✅ Админ статус

## 📝 Пошаговая настройка MongoDB Atlas (БЕСПЛАТНО)

### Шаг 1: Создание аккаунта

1. Перейди на https://www.mongodb.com/cloud/atlas/register
2. Зарегистрируйся (можно через Google)
3. Создай организацию (любое имя)

### Шаг 2: Создание бесплатного кластера

1. Нажми **"Build a Database"**
2. Выбери **"M0 FREE"** (512 MB бесплатно навсегда)
3. Выбери регион ближе к серверу Render (например, AWS / Frankfurt eu-central-1)
4. Имя кластера: `wave-chat-cluster`
5. Нажми **"Create"**

### Шаг 3: Настройка доступа

1. **Security Quickstart** → Username/Password:
   - Username: `wavechat`
   - Password: (создай сложный) - например `WaveChat2025!`
   - Нажми **"Create User"**

2. **Where would you like to connect from?**:
   - Выбери **"Add My Current IP Address"**
   - Добавь еще: **"Add a Different IP Address"**
   - IP: `0.0.0.0/0` (разрешить все - для Render)
   - Description: `Render Server`
   - Нажми **"Finish and Close"**

### Шаг 4: Получение Connection String

1. На главной странице кластера нажми **"Connect"**
2. Выбери **"Drivers"**
3. Driver: **Node.js**
4. Скопируй строку подключения, будет выглядеть так:
   ```
   mongodb+srv://wavechat:<password>@wave-chat-cluster.xxxxx.mongodb.net/?retryWrites=true&w=majority
   ```
5. Замени `<password>` на твой пароль (например `WaveChat2025!`)

### Шаг 5: Настройка на Render.com

1. Зайди на Render.com → твой сервис `wave-chat-server`
2. Перейди в **Environment**
3. Добавь новую переменную:
   - **Key:** `MONGODB_URI`
   - **Value:** (вставь твою connection string)
   ```
   mongodb+srv://wavechat:WaveChat2025!@wave-chat-cluster.xxxxx.mongodb.net/?retryWrites=true&w=majority
   ```
4. Нажми **"Save Changes"**
5. Render автоматически передеплоится

### Шаг 6: Проверка

После деплоя проверь логи Render:
- Должно быть: `Connected to MongoDB successfully`
- И при сохранении: `Data saved to MongoDB`

## ✅ Готово!

Теперь:
- ✅ История сообщений сохраняется навсегда
- ✅ Пользователи не теряются при редеплое
- ✅ Баны постоянные
- ✅ Бесплатно и без ограничений

## 🔍 Просмотр данных

1. MongoDB Atlas → **Database** → **Browse Collections**
2. Database: `wave-chat`
3. Collection: `chatData`
4. Там будет документ с `_id: "main"` со всеми данными

## 🚨 Если не хочешь настраивать MongoDB

Без MongoDB:
- ⚠️ История теряется при каждом деплое
- ⚠️ Пользователи восстанавливаются из localStorage (работает)
- ⚠️ Баны сбрасываются при редеплое

Код работает и без MongoDB, просто с ограничениями.
